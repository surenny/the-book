# OS
.DS_Store
Thumbs.db
*.swp
*~

# Editors
.vscode/
.idea/
*.sublime-*

# Local dev
node_modules/
.cache/
*.log

# Local-only reports (half-public hub):
#   everything in private/ (HTML files + reports.local.json) is local-only,
#   never pushed to git, never deployed to GitHub Pages.
/private/
